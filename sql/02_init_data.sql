-- ============================================================================
-- 上海救助基地潜水救生员训练系统 — 一期初始化数据脚本（空框架）
-- 数据库：rescue_training
-- 版本：V1.0   日期：2026-08-28
-- 说明：仅插入系统运行必需的基础数据（角色、顶级组织、超级管理员账号）。
--       不含任何业务数据（训练项目、题库、演练方案等，由后台批量导入）。
-- ============================================================================

USE `rescue_training`;

SET NAMES utf8mb4;

-- ---------------------------------------------------------------------------
-- 1. 初始化角色（四级权限体系）
-- ---------------------------------------------------------------------------
INSERT INTO `sys_role` (`id`, `role_code`, `role_name`, `data_scope`, `remark`) VALUES
  (1, 'SUPER_ADMIN', '超级管理员', 1, '基地平台总运维：全局配置、批量导入、全部门数据管理、高阶考核终审'),
  (2, 'DEPT_LEADER', '部门负责人', 2, '班组/分队负责人：本部门人员、训练、考核、团队实训统筹管理'),
  (3, 'RESCUER',     '潜水救生员', 3, '1-5级救生员：本级及以下训练/理论/考核内容');

-- ---------------------------------------------------------------------------
-- 2. 初始化顶级组织架构（组织树根节点）
-- ---------------------------------------------------------------------------
INSERT INTO `sys_dept` (`id`, `parent_id`, `dept_name`, `dept_type`, `sort`, `status`, `remark`) VALUES
  (1, 0, '上海救助基地', 1, 0, 1, '顶级组织根节点');

-- ---------------------------------------------------------------------------
-- 3. 初始化超级管理员账号
-- ---------------------------------------------------------------------------
-- 重要：password 字段为 BCrypt 密文，下方为占位符，必须在部署前替换。
-- 生成方式（二选一）：
--   A. 后端首次启动时，若检测到占位密码，自动重置并打印初始密码（推荐）。
--   B. 使用 Spring Security BCryptPasswordEncoder 生成密文后手动替换下方占位符。
INSERT INTO `sys_user`
  (`id`, `dept_id`, `username`, `password`, `real_name`, `phone`, `rescue_level`, `job_status`, `enabled`, `remark`)
VALUES
  (1, 1, 'admin', '__CHANGE_ME_BCRYPT__', '系统管理员', NULL, 5, 1, 1, '超级管理员初始账号，首次登录后请立即修改密码');

-- ---------------------------------------------------------------------------
-- 4. 超级管理员 - 角色关联
-- ---------------------------------------------------------------------------
INSERT INTO `sys_user_role` (`user_id`, `role_id`) VALUES (1, 1);

-- ---------------------------------------------------------------------------
-- 5. 超级管理员个人档案占位
-- ---------------------------------------------------------------------------
INSERT INTO `user_archive`
  (`user_id`, `current_level`, `total_train_hours`, `total_checkin`, `total_exam`)
VALUES
  (1, 5, 0.00, 0, 0);

-- ============================================================================
-- 初始化完成。业务数据（训练项目/题库/演练方案/普通账号等）请在 PC 后台
-- 通过「批量导入」功能或逐条录入完成，本脚本不再插入。
-- ============================================================================
