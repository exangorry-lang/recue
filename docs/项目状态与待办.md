# 项目状态与待办（断点续传用）

> 本文件是当前开发进度快照。重开会话后先读此文件即可无缝继续。

## 一、项目概况

- **项目**：上海救助基地潜水救生员训练系统（一期）
- **根目录**：`E:\上海救助基地潜水救生员训练APP`
- **技术栈**：Spring Boot 3.2.5 + MyBatis-Plus + MySQL 8｜PC = Vue3 + Element Plus｜安卓 = uni-app(Vue3)

## 二、四大部分（全部完成并验证）

| 部分 | 目录 | 状态 |
|---|---|---|
| 数据库 | `sql/`（01_schema / 02_init_data / 03_additional / 04_test_data） | ✅ 24 张表 + 测试数据 |
| 后端 | `backend/`（5 模块） | ✅ 编译/启动/接口验证通过 |
| PC 后台 | `pc-admin/` | ✅ build 通过，dist/ 可部署 |
| 安卓 APP | `rescue-app/` | ✅ H5 编译通过，已出 APK |

## 三、运行信息

- 三端：后端 `:8080`｜PC 后台 `:5174`｜安卓 H5 `:5173`
- 局域网 IP：**192.168.101.3**（⚠️ 换 WiFi 会变，需同步改 APP 的 BASE_URL 并重新打包）
- 一键启动：`start-all.bat`；备份：`backup.bat`

## 四、测试账号（密码均 admin123）

- `admin` 超管｜`zhangwei` 部门负责人(5级)
- 救生员：`lihua`(1级)/`wangqiang`(2级)/`zhaomin`(3级)/`chenjing`(4级)/`liuyang`(5级)

## 五、关键环境

- Java 17：`E:\java\jdk 17`
- Maven 3.9.9：`%USERPROFILE%\.m2\wrapper\dists\...\bin\mvn.cmd`（用 Get-ChildItem 搜索）
- MySQL 8.0：`D:\MySQL\MySQL Server 8.0\bin`，root / root123456，库名 `rescue_training`
- Node/pnpm/npm 可用；Git 已配远程 GitHub，代理 `127.0.0.1:7892`

## 六、待办事项

1. 【未推送】Git 有多笔 commit 未 push（需开代理 7892）
2. 【待决定】是否把「团队任务列表/考试场次列表」也做部门数据隔离
3. 【待决定】`getUserAll` 接口是否加权限限制
4. 【可选】把「IP 会变需重新打包」「打包→安装→登录」流程写进操作手册第九章
5. 【可选】PC 端部署手册（Nginx + dist + 反向代理）

## 七、已完成的重要功能（近期）

- 权限：PC 仅超管+负责人登录；管理接口 @RequireRole 角色校验；打卡审核部门数据隔离
- 考试：自动组卷、判分、简答题人工阅卷、成绩归档一致
- 团队：负责人 5 级校验、参与打分、演练复盘、我的团队评分、打卡去重
- 个人打卡：去重 + 审核状态反馈
- 晋升：目标等级动态下拉
- 文档：操作手册 V1.2、导入模板（JSON + Excel）

## 八、文档与脚本位置

- 操作手册：`docs/操作手册.docx`（生成脚本 `docs/generate_manual.py`）
- 导入模板：`templates/题库导入示例.json`、`题库导入模板.xlsx`、`训练项目导入模板.xlsx`
